from __future__ import unicode_literals
import dataent

def execute():
	return
	# dataent.reload_doc('website', 'doctype', 'style_settings')
	# style_settings = dataent.get_doc("Style Settings", "Style Settings")
	# if not style_settings.apply_style:
	# 	style_settings.update(default_properties)
	# 	style_settings.apply_style = 1
	# 	style_settings.save()
